# frozen_string_literal: true

RSpec.describe Rubidi do
  it "has a version number" do
    expect(Rubidi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
